{
  "name": "bs-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "cross-env BS_ENV=prod pnpm -r -parallel run build",
    "serve": "cross-env BS_ENV=dev1 pnpm -r run serve"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
